
    <style>
        /* Reset básico */



        /* ===== FOOTER MODERNO ===== */
        .footer-preguntame {
            background: linear-gradient(135deg, #1a237e 0%, #0d47a1 50%, #1565c0 100%);
            color: white;
            padding: 3rem 2rem 1.5rem;
            position: relative;
            overflow: hidden;
            margin-top: 230px;
        }

        /* Efecto de ondas en el fondo */
        .footer-preguntame::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg,
            #00ffff 0%,
            #667eea 25%,
            #764ba2 50%,
            #667eea 75%,
            #00ffff 100%
            );
            background-size: 200% 100%;
            animation: gradientShift 3s ease infinite;
        }

        @keyframes gradientShift {
            0%, 100% { background-position: 0% 0%; }
            50% { background-position: 100% 0%; }
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            gap: 2rem;
        }

        /* Sección superior del footer */
        .footer-top {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            padding-bottom: 2rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        }

        /* Logo y descripción */
        .footer-brand {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .footer-logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-size: 1.5rem;
            font-weight: bold;
        }

        .footer-logo-img {
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, #ffd93d 0%, #f9a825 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .footer-description {
            color: rgba(255, 255, 255, 0.8);
            font-size: 0.95rem;
            line-height: 1.6;
        }

        /* Enlaces rápidos */
        .footer-section {
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
        }

        .footer-section h3 {
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
            color: #00ffff;
        }

        .footer-links {
            list-style: none;
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .footer-links a {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .footer-links a:hover {
            color: #00ffff;
            transform: translateX(5px);
        }

        .footer-links a::before {
            content: '▸';
            font-size: 0.8rem;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .footer-links a:hover::before {
            opacity: 1;
        }

        /* Redes sociales */
        .social-links {
            display: flex;
            gap: 1rem;
        }

        .social-link {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            text-decoration: none;
            font-size: 1.2rem;
            transition: all 0.3s ease;
            backdrop-filter: blur(10px);
        }

        .social-link:hover {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 255, 255, 0.4);
        }

        /* Sección inferior */
        .footer-bottom {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
            padding-top: 1.5rem;
        }

        .copyright {
            color: rgba(255, 255, 255, 0.7);
            font-size: 0.9rem;
        }

        .footer-meta {
            display: flex;
            gap: 1.5rem;
        }

        .footer-meta a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            font-size: 0.9rem;
            transition: color 0.3s ease;
        }

        .footer-meta a:hover {
            color: #00ffff;
        }

        /* Badge especial */
        .made-with-love {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.5rem 1rem;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            backdrop-filter: blur(10px);
            font-size: 0.85rem;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .heart {
            color: #ff4d6d;
            animation: heartbeat 1.5s ease-in-out infinite;
        }

        @keyframes heartbeat {
            0%, 100% { transform: scale(1); }
            10%, 30% { transform: scale(1.1); }
            20%, 40% { transform: scale(1); }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .footer-preguntame {
                padding: 2rem 1.5rem 1rem;
            }

            .footer-top {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }

            .footer-bottom {
                flex-direction: column;
                text-align: center;
            }

            .footer-meta {
                flex-direction: column;
                gap: 0.75rem;
            }
        }
    </style>




<footer class="footer-preguntame">
    <div class="footer-content">
        <!-- Sección superior -->
        <div class="footer-top">
            <!-- Marca y descripción -->
            <div class="footer-brand">
                <div class="footer-logo">
                    <div class="footer-logo-img"><img src="{{BASE_URL}}public/img/preguntame-logo-solo.png" width="50px" alt=""></div>
                    <span>Preguntame</span>
                </div>
                <p class="footer-description">
                    Pon a prueba tus conocimientos con el mejor juego de trivia.
                    Desafía a tus amigos y conviértete en el campeón del saber.
                </p>
                <div class="made-with-love">
                    Hecho con <span class="heart">♥</span> para aprender
                </div>
            </div>

            <!-- Enlaces rápidos -->
            <div class="footer-section">
                <h3>🎯 Menú</h3>
                <ul class="footer-links">
                    <li><a href="{{BASE_URL}}">Inicio</a></li>
                    <li><a href="{{BASE_URL}}PartidaController/iniciar">Jugar</a></li>
                    <li><a href="{{BASE_URL}}PreguntaController/mostrarFormularioSugerir">Sugerir Pregunta</a></li>
                    <li><a href="{{BASE_URL}}PerfilController/verPerfil">Mi Perfil</a></li>
                </ul>
            </div>

        

            <!-- Redes sociales -->
            <div class="footer-section">
                <h3>🌐 Síguenos</h3>
                <div class="social-links">
                    <a href="#" class="social-link" title="Facebook">
                        <span>f</span>
                    </a>
                    <a href="#" class="social-link" title="Twitter">
                        <span>𝕏</span>
                    </a>
                    <a href="#" class="social-link" title="Instagram">
                        <span>📷</span>
                    </a>
                    <a href="#" class="social-link" title="Discord">
                        <span>💬</span>
                    </a>
                </div>
            </div>
        </div>

        <!-- Sección inferior -->
        <div class="footer-bottom">
            <p class="copyright">
                © 2025 Preguntame - Demo básica para refactor
            </p>
            <div class="footer-meta">
                <a href="#">Términos de Servicio</a>
                <a href="#">Política de Privacidad</a>
                <a href="#">Cookies</a>
            </div>
        </div>
    </div>
</footer>
