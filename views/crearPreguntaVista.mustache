<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crear Pregunta</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">

</head>

<body>

{{> header}}

<div class="container mt-4">

    <h2>Crear Nueva Pregunta</h2>

    <form action="{{BASE_URL}}EditorController/guardarPregunta" method="POST" class="card p-4 shadow-sm mt-3">

        <div class="mb-3">
            <label class="form-label">Pregunta</label>
            <input type="text" name="pregunta" class="form-control" required>
        </div>

        <div class="mb-3">
            <label class="form-label">Categoría</label>
            <select name="categoria" class="form-select" required>
                {{#categorias}}
                    <option value="{{categoria}}">{{categoria}}</option>
                {{/categorias}}
            </select>
        </div>

        <div class="mb-3">
            <label class="form-label">Puntaje</label>
            <input type="number" name="puntaje" class="form-control" value="10" min="1" required>
        </div>

        <h4>Respuestas</h4>

        {{#respuestas}}
        <div class="input-group mb-2">
            <span class="input-group-text">Respuesta {{n}}</span>
            <input type="text" name="respuestas[]" class="form-control" required>
            <span class="input-group-text">
                <input type="radio" name="correcta" value="{{index}}" required>
                Correcta
            </span>
        </div>
        {{/respuestas}}

        <button type="submit" class="btn btn-success mt-3">Crear Pregunta</button>

    </form>

    <div class="mt-4">
        <a href="{{BASE_URL}}EditorController/home" class="btn btn-secondary">⬅ Volver</a>
    </div>

</div>

{{> footer}}

</body>
</html>
