<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Reportar Pregunta</title>
    <link rel="stylesheet" href="{{basePath}}public/css/estilos.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="icon" type="image/png" href="{{basePath}}public/img/preguntame-logo-solo.png">
<style>
        .form-label, .form-select, .form-control, .btn {
            font-size: 1.1rem; /* Base un poco más grande que el default */
        }
        
        /* * Media Query para dispositivos pequeños (max 576px, típico de móviles)
          * AUMENTADO EL TAMAÑO DE FUENTE DE LABELS/CONTROLES A 1.3rem
          */
        @media (max-width: 576px) {
            /* 1. Ajustar el tamaño del título */
            h1 {
                font-size: 2rem !important; 
                margin-top: 1rem !important;
            }

            /* 2. Ajustar tamaño de labels y controles (Aumentado a 1.3rem para máxima legibilidad) */
            .form-label, .form-select, .form-control {
                font-size: 1.3rem !important; /* <-- FIX CLAVE: Subido a 1.3rem */
            }
            
            /* 3. Ajustar el layout de los botones (div con clase .text-center) */
            .text-center {
                display: flex !important; 
                flex-direction: column !important; /* Apilamos los botones verticalmente */
                gap: 10px !important; 
                padding-bottom: 10px !important; 
            }

            /* Hacemos que los botones ocupen todo el ancho y tengan buen padding */
            .text-center .btn {
                width: 100% !important; 
                padding: 12px !important; /* Un poco más de padding para mejor toque */
                font-size: 1.2rem !important;
            }

            /* Ajuste el contenedor general y el card para mejor visualización móvil */
            .container {
                padding-left: 10px !important; 
                padding-right: 10px !important; 
                margin-top: 1rem !important;
            }
            .card {
                padding: 20px !important; /* Un poco más de padding interno en el card */
            }
        }
    </style>
</head>

<body class="container mt-5">
    <h1 class="mb-4 text-center text-danger">Reportar Pregunta</h1>

    <form action="{{basePath}}ReportarPreguntaController/guardarReporte" method="POST" class="card p-4 shadow">
        <input type="hidden" name="id_partida" value="{{id_partida}}">

        <div class="mb-3">
            <label for="id_pregunta" class="form-label">Seleccioná la pregunta a reportar:</label>
            <select name="id_pregunta" id="id_pregunta" class="form-select" required>
                <option value="">-- Elegir pregunta --</option>
                {{#preguntas}}
                <option value="{{id_pregunta}}">{{pregunta}}</option>
                {{/preguntas}}
            </select>
        </div>

        <div class="mb-3">
            <label for="motivo" class="form-label">Motivo del reporte:</label>
            <textarea name="motivo" id="motivo" class="form-control" rows="4" placeholder="Explicá por qué reportás esta pregunta..." required></textarea>
        </div>

        <div class="text-center">
            <button type="submit" class="btn btn-danger">Enviar Reporte</button>
            <a href="{{basePath}}" class="btn btn-secondary">Cancelar</a>
        </div>
    </form>
</body>
</html>
