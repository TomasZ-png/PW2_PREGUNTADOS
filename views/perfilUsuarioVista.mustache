<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Perfil 🌟</title>
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">

    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    <style>
        #map {
            width: 100%;
            height: 350px;
            border-radius: 10px;
            margin-top: 15px;
            position: relative;
            z-index: 10;
        }
        .animated-bg { z-index: -1 !important; }
    </style>
</head>

<body>


<div class="container mt-5">

    <h1 class="text-center mb-4" style="margin-top: 100px;">👤 Perfil de Usuario</h1>

    <div class="card shadow p-4 mx-auto" style="max-width: 600px;">
        <div class="text-center">
            <img src="{{BASE_URL}}public/img/user-img/{{perfil.foto_perfil}}"
                 alt="Foto de perfil"
                 class="rounded-circle mb-3"
                 width="150" height="150">
        </div>

        <h3 class="text-center mb-3"> <strong>Usuario:</strong> {{perfil.nombre_completo}} </h3>

        <ul class="list-group">
            <li class="list-group-item">
                <strong>📧 Correo:</strong> {{perfil.correo}}
            </li>

            <li class="list-group-item">
                <strong>🎂 Año de nacimiento:</strong> {{perfil.anio_nacimiento}}
            </li>

            <li class="list-group-item">
                <strong>🧬 Sexo:</strong> {{perfil.sexo}}
            </li>

            <li class="list-group-item">
                <strong>🏆 Puntaje Máximo:</strong>
                <span class="text-success fw-bold">{{perfil.puntaje_maximo_obtenido}}</span>
            </li>

            <li class="list-group-item">
                <strong>📍 Dirección:</strong>
                <span class="text-success fw-bold">{{direccion.ciudad}}, {{direccion.pais}}</span>
            </li>

            <li class="list-group-item">
                <a href="{{BASE_URL}}PerfilController/historial?id_usuario={{perfil.id_usuario}}"><strong> Historial de Partidas</strong></a>
            </li>
        </ul>

        <div id="map" class="mt-3"></div>

        <div class="text-center mt-4">
            <a href="{{BASE_URL}}" class="btn btn-primary">⬅ Volver al Home 🏠</a>
        </div>
    </div>

</div>


<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>

<script>
    document.addEventListener("DOMContentLoaded", function() {
        const latRaw = {{direccion.latitud}} || 'null';
const lngRaw = {{direccion.longitud}} || 'null';

if (latRaw === null || lngRaw === null) {
    console.warn("No hay coordenadas para este usuario.");
    document.getElementById('map').innerHTML = "<p class='text-center mt-3'>Ubicación no cargada.</p>";
return;
}

    const lat = parseFloat(latRaw);
    const lng = parseFloat(lngRaw);

    const map = L.map('map').setView([lat, lng], 13);

    // capa base
    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
        maxZoom: 19,
        attribution: '&copy; OpenStreetMap contributors'
    }).addTo(map);

    // marcador
    L.marker([lat, lng]).addTo(map)
    .bindPopup("📍 Ubicación del usuario")
    .openPopup();

    setTimeout(function() {
        try {
            map.invalidateSize();
        } catch (e) {
            console.error("invalidateSize error", e);
        }
    }, 400);
    });
</script>

</body>
</html>
